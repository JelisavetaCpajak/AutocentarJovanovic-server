Server side of the applicatio is done using node.js. To start server , run "npm run".
In this repository there is also sql script for database, under name "autocentar.sql". You need to set your credentials for database in file index.js.
